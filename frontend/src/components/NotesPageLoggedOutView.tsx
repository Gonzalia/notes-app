import classes from "../styles/utils.module.css";
const NotesPageLoggedOutView = () => {
  return <p className={classes.whiteText}>Please login to see your notes</p>;
};

export default NotesPageLoggedOutView;
